export type NavDataType = {
  href: string;
  description: string;
  name: string;
  speed: number;
  index: number;
};

export type SpotifyDataType = {
  band: string;
  song: string;
  url: string;
};
